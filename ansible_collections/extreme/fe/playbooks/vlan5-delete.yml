---
- name: Delete VLAN 5 on ExtremeNetworks Fabric Engine switches
  hosts: gns3_network
  gather_facts: false
  tasks:
    - name: Remove VLAN 5 if present
      extreme.fe.extreme_fe_vlans:
        vlan_id: 5
        vr_name: GlobalRouter
        state: deleted
