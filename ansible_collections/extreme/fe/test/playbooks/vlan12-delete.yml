---
- name: Delete VLAN 12 on ExtremeNetworks Fabric Engine switches
  hosts: fe_dt_lower_hosts
  gather_facts: false
  tasks:
    - name: Remove VLAN 12 if present
      extreme.fe.extreme_fe_vlans:
        vlan_id: 12
        vr_name: GlobalRouter
        state: deleted
