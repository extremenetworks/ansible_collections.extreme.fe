# Integration test placeholder
# This directory contains integration test targets for ansible-test integration

# Note: Integration tests require physical or emulated Extreme Networks devices.
# They are not run in CI by default.

# To run integration tests locally:
# ansible-test integration --docker -v

# Test targets should be placed in subdirectories following the pattern:
# tests/integration/targets/<target_name>/
#   tasks/
#     main.yml
